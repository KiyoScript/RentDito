 <tr>
  <td><%= feedback.suggestion %></td>
  <td class="<%'d-none' if current_user.landlord? || current_user.admin? %>"><%= feedback.user.fullname %></td>
  <td><%= distance_of_time_in_words(feedback.created_at, feedback.updated_at).titleize %></td>
</tr>
